{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'h=\"${CLAUDE_PLUGIN_ROOT:-}/scripts/rtk-rewrite.sh\"; [ -x \"$h\" ] && exec \"$h\"; exit 0'"
          }
        ]
      }
    ]
  }
}
